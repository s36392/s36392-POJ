import java.util.*;
import java.io.*;
import java.math.*;

/**
 * Auto-generated code below aims at helping you parse
 * the standard input according to the problem statement.
 **/
class Solution {

    /*
    public static void main(String args[]) {
        Scanner in = new Scanner(System.in);
        int n = in.nextInt(); // the number of temperatures to analyse
        for (int i = 0; i < n; i++) {
            int t = in.nextInt(); // a temperature expressed as an integer ranging from -273 to 5526
        }

        // Write an answer using System.out.println()
        // To debug: System.err.println("Debug messages...");

        System.out.println("result");
    }
    */

    public static void main(String args[]) {
        Scanner scanner = new Scanner(System.in);
        int index = scanner.nextInt(); // the number of temperatures to analyse
        int[] temperatures = new int[n];
        for (int i = 0; i < index; i++) {
            temperatures[i] = scanner.nextInt(); // a temperature expressed as an integer ranging from -273 to 5526
        }

        // Write an answer using System.out.println()
        // To debug: System.err.println("Debug messages...");

        if (n == 0) {
            System.out.println(0);
        } else {
            int closest = temperatures[0];
            for (int i = 1; i < n; i++) {
                int absT = Math.abs(temperatures[i]);
                int absClosest = Math.abs(closest);
                if (absT < absClosest || (absT == absClosest && temperatures[i] > 0)) {
                    closest = temperatures[i];
                }
            }
            System.out.println(closest);
        }

        scanner.close();
    }
}